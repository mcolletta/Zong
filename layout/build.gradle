apply plugin: 'java'
sourceCompatibility = 6

dependencies {
	compile project(':core')
	compile project(':utils-jse')
	compile project(':utils-pdlib')
}

sourceSets {
	main {
		java { srcDir 'src' }
		resources { srcDir 'src' }
	}
}